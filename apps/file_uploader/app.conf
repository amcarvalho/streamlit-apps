# Specify the name/title of the Application end-users will see
name = "CSV File Uploader"

# Specify a list of roles which will have access to this application
allowed_roles = ['dbt_developer_role']

# Uncomment the line below to specify a compute warehouse which will run the application.
# You need to ensure that the roles defined above have can use the warehouse specified below.
# warehouse = "compute_wh"
